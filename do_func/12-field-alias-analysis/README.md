# 12 字段别名、锁所有权与回调候选

本次简报：新增 `&object → pointer → pointer-copy` 有限闭包、单层字段访问路径、实参到形参对象传播、形参锁所有权摘要、函数指针字段候选、`aliases` CLI、独立阶段产物和 HTML 视图。

边界：分析在函数内是流不敏感的；数组、指针算术、复杂强制转换、嵌套字段精确路径、多目标消歧、回调注销及调用方临界区展开尚未完成。
