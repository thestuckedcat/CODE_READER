# 11 锁与共享状态并行性分析

从 Clang 语义中提取锁事件和词法临界区，将静态/全局共享变量访问关联到持有锁集合，并比较冲突访问是否由共同锁串行化。`potential_race` 仅表示缺少共同锁且可能并行，不证明运行时一定发生竞争。

本次简报：已新增快照字段、`locks` CLI、HTML 锁视图、独立产物 `lock_analysis.json` 和本地最小验收。当前边界是不证明线程可达性、happens-before、条件变量与跨函数锁传递。
