# Design

Detect CMake, Ninja, Git, compilers, libclang and optional native CFG support.
The function reports capability state and never installs or mutates dependencies.
Installation is a separate repository-local runtime operation.
