# 13 多目标嵌套字段、回调状态与锁包装函数

本次简报：别名分析可保守保留多个对象目标，字段访问记录完整成员链；回调字段赋值形成 set/clear 事件与最终状态；单一净 acquire/release 的简单包装函数可投影到调用方临界区，并用于共享变量并行性分析。

边界：多目标结果是 `may` 而非路径消歧；回调顺序仅按同函数源码位置；锁包装投影不解释分支、递归、异常、RAII 或复杂净效应；不证明线程在运行时重叠。
