# 14 虚调用与继承候选

本次简报：提取 C++ 类型继承边、方法所属类型及 `virtual`/`final` 证据；虚调用沿已解析派生类型枚举签名匹配候选，并发布 `static_exact`、`closed_by_final` 或 `open_world` 候选集状态。

边界：仓库枚举不等于完整程序，非 `final` 类型始终保留未知外部/未解析派生类型；覆盖匹配当前按方法名和参数类型限定，不覆盖所有模板、协变返回、using 声明和 ABI 去虚化规则。
