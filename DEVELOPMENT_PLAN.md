# CODE_READER 开发计划与功能验收矩阵

更新日期：2026-09-10。当前版本 SDK Code Atlas 0.6。第 1 轮 v0.2 与第 2 轮 v0.3 的功能状态分别见 [update/001_round1_changes.md](update/001_round1_changes.md)、[update/002_round2_changes.md](update/002_round2_changes.md)；v0.4 分层重构见 [update/003_refactor_changes.md](update/003_refactor_changes.md)。v0.5 完成锁分析限定子集；v0.6 完成单层字段、有限别名、参数对象传播、锁所有权摘要和回调字段候选。多目标精确化、线程可达性、完整 happens-before、复杂路径及异常语义仍未完成。
代码基线：[389ab62](https://github.com/thestuckedcat/CODE_READER/commit/389ab627736fecfddb792d800d69b88718133af5)；总体设计根目录补充：[d734720](https://github.com/thestuckedcat/CODE_READER/commit/d7347201f9317e8a26d436df72be22f34334ee46)。

本文跟踪当前交付与后续六轮开发。每一轮是可独立验收的阶段，可拆成多个 PR；不是六次对话或承诺六个固定工期。当前基础原型记为第 0 轮，目前第 1 轮已完成限定范围技术验证，第 2 轮已交付标量子集；完整产品验收与后续阶段仍未完成。

设计依据：[总体设计 v3.1](SDK_Code_Atlas_Design_v1.md)、[当前实现能力](references/capabilities.md)、[现有测试报告](TEST_REPORT.md)。文档出现“预期结果”不表示已经执行验证。

## 1. 状态与完成标准

| 标记 | 是否完成 | 含义 |
|---|---|---|
| 已完成（限定范围） | 是，仅限所列范围 | 已实现且有可复现验证；不能推广到未覆盖场景 |
| 部分完成 | 否 | 有基础实现，仍缺语义能力、边界处理或验收 |
| 已实现待验证 | 否 | 代码或打包已准备，尚无目标环境实测证据 |
| 未开始 | 否 | 计划项；不在 UI 或说明中宣称支持 |
| 待用户验收 | 否 | 实现和技术验证已通过，等待功能/架构决策；不得自动勾选 |

整轮完成必须满足：本轮所有必需功能验收通过；未支持语义有显式状态；新增产物具备缓存依赖与失效规则；验证结果记录了 commit、配置、命令、实际输出及限制。运行状态 succeeded 不等于完整调用链已证明。

## 2. 第 0 轮：当前原型实际支持情况

本轮交付状态：基础可执行原型已交付；完整产品需求未完成。下表的验证依据来自现有 7 组自动化测试、独立前向测试和 llama.cpp 实际运行，不是本次文档提交重新执行的测试。

| ID | 本轮功能及支持范围 | 是否完成 / 当前状态 | 验证方法 | 预期结果 | 已有实际结果 / 限制 |
|---|---|---|---|---|---|
| R0-01 | Skill、Python CLI、Linux x64 包内依赖启动 | 是，限定 Linux 测试环境 | 完整 ZIP 解压到另一含空格路径，执行 `bash run.sh doctor` | 加载包内 Python/libclang/CMake/Ninja，不依赖原打包绝对路径 | 通过；不代表任意 Linux 发行版/架构均支持 |
| R0-02 | Windows x64 运行时与启动器 | 是，限定当前测试环境 | 在 Windows x64 隔离运行时执行 doctor、完整 fixture、export 与 DOM 测试 | 正常启动、解析和导出，错误有明确提示 | 2026-09-08/09 实测通过；不代表所有 Windows 版本或无编译器主机 |
| R0-03 | CMake 求值及显式参数输入 | 部分完成 | fixture 配置；llama.cpp 显式配置；核对 compdb 和 build_context | 使用 CMake 的 include、宏和命令；配置失败停止并保留日志 | 简单项目及 llama.cpp 已运行；复杂 superbuild 的自动调查/闭环待第 1 轮 |
| R0-04 | 多源码根、多个编译数据库入口 | 部分完成 | 构造主仓调用依赖仓函数、两个独立子构建的样例 | 跨仓声明与定义正确关联，各自编译上下文保留 | 已有重复 `--root/--compdb` 接口；完整跨仓专项验收未完成 |
| R0-05 | 函数、类型、字段、静态/全局对象及直接调用提取 | 是，限定 fixture / 已解析 TU | `test_01_calls_and_types`；检查 Clang 证据 | sdk_entry→leaf、alternate→leaf 正确；Config 字段存在；settings 为静态对象 | 通过；未保证所有 C++ 语法与配置变体 |
| R0-06 | 重载区分、虚调用与间接调用未知标记 | 是，限定测试场景 | `test_01`、`test_03`，查询 overload 与虚调用 | 两个 overload 身份不同；名称歧义要求选择；虚派发不冒充 exact | 通过；复杂指针/继承目标分析尚未完成 |
| R0-07 | 接口进入、按需扩大 TU、向上路径首次分歧 | 部分完成 | fixture 选择 sdk_entry；`trace --function leaf --direction up` | 不解析无关 cpp.cpp；返回 sdk_entry/alternate 首次分歧 | 前向测试通过；调用范围仍受候选发现、间接目标与预算限制 |
| R0-08 | 参数绑定、赋值/计算表达式、返回表达式 | 部分完成 | `test_01` 检查 argument_binding；人工核对 flow_edges | 展示实参来源和字段/变量赋值，无法证明部分显示 may | 有路径不敏感图；没有完整 CFG/别名/跨函数返回求解 |
| R0-09 | 对象引用、写入和初始化位置 | 部分完成 | 核对 settings 的源码、objects/state_events | 对象单独可见，事件带源码位置与所属函数 | 已有基础提取；专用注册/注销生命周期及精确运行顺序未完成 |
| R0-10 | 审阅请求、结构/证据校验、候选单独导入 | 是，限定协议验证 | `test_04_invalid_review_and_valid_candidate` | 拒绝旧输入、源码变化、重复导入；同轮不同任务可导入；建议保持 agent/may | 通过；测试仅证明协议，不证明 Agent 建议的业务语义正确 |
| R0-11 | TU 增量缓存与头依赖 | 是，限定测试场景 | `test_02_cache_and_incremental` | 无改动 0 解析/3 复用；改 other.c 为 1/2；改共享头为 2/1 | 通过；全阶段精细依赖维护仍待完善 |
| R0-12 | 新文件与 include 环境变化失效 | 是，保守策略 | `test_06_negative_dependency`、`test_07_semantic_environment_invalidates_cache` | 新增无扩展名头、修改 CPATH 不复用失效 AST | 通过；可能扩大重解析范围，尚非最小失效集合 |
| R0-13 | 解析失败移除旧事实 | 是，限定缺头场景 | `test_05_parse_failure_does_not_reuse_old_facts` | 缺头 TU 失败；新快照不继续保留旧 leaf 定义；保留诊断 | 通过；更多崩溃窗口需第 5 轮专项验证 |
| R0-14 | 内容寻址、checkpoint、写者锁、原子快照 | 部分完成 | 检查已提交 TU 复用；后续增加各阶段故障注入 | 重启复用已提交任务，半成品不成为 current | 基础机制已实现；完整中断/损坏/并发恢复验收未完成 |
| R0-15 | 离线 HTML、黑盒详情、对象/类型框、证据和数据流展示 | 部分完成 | `test_03_paths_and_export`；Node JS 语法检查；后续浏览器交互验收 | 不依赖 CDN/fetch；选择函数和展开时显示对应数据 | 导出/语法检查通过；浏览器交互尚未实测，不能标为全部完成 |
| R0-16 | 函数功能简介 | 部分完成 | 有/无文档注释的函数对照 | 有注释展示来源；无注释明确待审阅，不猜测功能 | 当前优先源码注释；独立简介任务闭环未完成 |
| R0-17 | llama.cpp 实际接入 | 是，限定预算与配置 | 固定 commit `73a43d1f69345aee8bb186ef4b3172cef892f2e5`，接口 llama_model_load_from_file，max-tu=8 | 无解析错误；界面和数据均声明范围与未完成任务 | 显式关闭 CPU/OpenMP 等选项的分析配置：8 TU，18 函数，38 调用目标边，14 审阅任务；结果 partial |
| R0-18 | 完整 CFG、别名固定点、Linux 专用规则、正式 VSCode 插件 | 否，未开始 | 分别按后续第 2、3、4、6 轮验收 | 达到对应阶段表的预期行为 | 当前版本不支持，不因已有底层记录而标完成 |

现有测试入口：完整运行时已准备时执行 `bash run.sh selftest`；源码仓库本身不含 runtime，准备方式见 [README](README.md)。测试函数对应 [tests/test_pipeline.py](tests/test_pipeline.py)。

## 3. 后续六轮总览

| 轮次 | 核心目标 | 当前整轮状态 | 主要依赖 | 阶段出口 |
|---|---|---|---|---|
| 第 1 轮 | 工程接入与轻量接口调用链 | 限定范围技术验证通过，真实 SDK 待验收 | 第 0 轮 | 接口优先场景技术验收与用户试用 |
| 第 2 轮 | 专用提取器、完整控制流、参数计算链 | 标量子集已交付，完整精度边界未完成 | 第 1 轮稳定编译上下文 | 支持子集内的条件化参数来源链 |
| 第 3 轮 | 指针/字段/回调、审阅和简介 | 未完成，仅基础交接已有实现 | 第 2 轮 IR/摘要 | 结构体与回调链具备证据、候选及限制 |
| 第 4 轮 | Linux SDK 边界与对象生命周期 | 未开始 | 第 3 轮字段与回调基础 | 核心业务功能对真实 SDK/驱动样例验收 |
| 第 5 轮 | 全阶段增量、恢复、HTML、平台与原型验收 | 未完成，有基础缓存/HTML | 第 1—4 轮 | 用户确认功能并对齐正式架构 |
| 第 6 轮 | 正式 VSCode 插件 | 未开始，受验收门禁约束 | 第 5 轮用户验收及架构确认 | 可安装、可在远端工作区运行的扩展 |

增量维护是各轮共同要求，不拖到第 5 轮才实现。新增 CFG、摘要、规则、Agent 结果时，必须同时定义输入依赖、缓存键、失效传播、checkpoint 与导出状态。

## 4. 第 1 轮：接入与轻量调用链（最高优先级）

| ID | 本轮支持功能 | 是否完成 / 当前状态 | 验证方法（计划） | 预期结果 / 验收标准 |
|---|---|---|---|---|
| R1-01 | 环境能力检查、可继续/阻塞分类 | 部分完成 | 分别移除解析器、CMake、业务头和编译数据库 | 精确指出受阻阶段与补齐方式，不静默改用文本搜索声称成功 |
| R1-02 | superbuild 入口/子构建调查，CMake 自己求值 | 部分完成 | add_subdirectory 与独立 ExternalProject 两组 fixture；与真实 compdb 比较 | 纳入所有选定 target 的上下文；未配置的子构建显式列缺项，不能伪造命令 |
| R1-03 | TOP_DIR 等参数发现、一次性集中提问与假设登记 | 部分完成 | 缺路径、缺产品宏、已有有效值三组用例 | 只询问未知语义参数；缓存值有效时不重问；替代配置带来源/影响 |
| R1-04 | 跨仓依赖、生成头、架构上下文 | 部分完成 | 两仓同名头、不同宏、生成配置头缺失/补齐对照 | include 顺序正确；配置变体不混用；补齐后只失效相关任务 |
| R1-05 | 任意接口按需解析与范围证明 | 部分完成 | fixture、llama.cpp、用户 SDK；与选定配置的全量参考结果比较 | 接口子图一致；无依赖的 TU 不解析；frontier、遗漏风险、预算截断可见 |
| R1-06 | 上下游路径、首次分歧、重载与递归 | 部分完成 | 菱形调用、共享子树、递归、多个同名入口与多个 callsite | 从用户选定方向列首次分歧；保留后续选择；递归折叠而非无限展开 |
| R1-07 | 基础阶段增量与缓存解释 | 部分完成 | 重复运行、依赖仓改头、改宏、改路径映射 | 可查看每个 TU 的复用/重算原因，不能仅按主仓 commit 判断缓存有效 |

本轮产物：工程接入配置协议、轻量查询命令/UI、跨仓与 superbuild 样例、与参考结果对照报告。性能记录环境、TU 数、耗时、内存；在数据到位前不承诺固定秒数。

## 5. 第 2 轮：控制流与参数计算链

| ID | 本轮支持功能 | 是否完成 / 当前状态 | 验证方法（计划） | 预期结果 / 验收标准 |
|---|---|---|---|---|
| R2-01 | 专用 C++/Clang 提取器及 Python 驱动 | 未开始 | 与现有 fixture 事实对照；校验输出 schema、退出状态和证据 | 保持直接调用事实；输出真实 CFG/IR；不支持的节点保留 opaque |
| R2-02 | 分支、汇合、循环、早返回的 CFG | 未开始 | if/switch/for/while/break/continue/return/goto、支持范围内异常样例 | 正确表示控制边与条件；显式声明异常/语言特性支持范围 |
| R2-03 | 定义—使用链与覆盖赋值 | 未开始 | `x=input; x=0; send(x)` 及双分支赋值 | 不将被无条件覆盖的 input 当作 send 的有效值来源；分支来源有条件 |
| R2-04 | 参数/返回值跨函数摘要 | 部分完成，只有基础绑定 | 多层 `f→g→h` 算术返回、递归 SCC 与副作用样例 | 参数到返回和调用点结果可连接；固定点收敛或预算不足明确可见 |
| R2-05 | 按单个参数/字段请求计算链 | 部分完成 | 从顶层实参、内部形参、返回值分别请求 | 输出可追溯的计算/赋值链；未请求能力显示 not_requested |
| R2-06 | CFG/摘要的增量维护 | 未开始 | 修改函数体但签名不变；修改摘要可见行为两种对照 | 重解析相关 TU；仅让依赖变化摘要的调用者任务失效 |

本轮不以“找到所有文本赋值”代替正确的数据流；无法判定路径可行性时保留条件与 may。入口参数条件由用户显式给出，不猜业务取值。

## 6. 第 3 轮：指针、结构体与回调

| ID | 本轮支持功能 | 是否完成 / 当前状态 | 验证方法（计划） | 预期结果 / 验收标准 |
|---|---|---|---|---|
| R3-01 | 地址获取、指针拷贝、参数传指针的别名分析 | 已完成（限定单目标、无复杂转换） | `p=&obj; q=p; q->x=v` 与实参到形参 fixture | 单目标输出 exact；数组、算术、转换和多目标仍为 may/unknown |
| R3-02 | 字段敏感传播与全局对象副作用 | 部分完成，单层字段 | 同对象不同字段、指针字段与直接字段访问 | 单层字段不混淆；嵌套字段、数组下标与复杂类型转换仍待实现 |
| R3-03 | 函数指针写入、注册与调用目标 | 部分完成，单层注册候选 | callback 字段经参数注册具体函数 | 输出有证据候选；条件覆盖、注销与多级转发仍未完成 |
| R3-04 | 虚调用与继承候选 | 部分完成，仅保守 may | 基类指针、多个派生类、限定调用、开放类型集合 | 区分静态确定目标与动态候选；未完整覆盖的类集合不宣称唯一 |
| R3-05 | 问题分组、Subagent 补充及配置重解析闭环 | 部分完成 | 同槽位多个调用点、矛盾提案、缺配置提案、无新证据重试 | 按共同原因去重；事实不可被模型覆盖；配置修正回到 Clang；无进展停止 |
| R3-06 | 函数功能简介与黑盒契约 | 部分完成，仅源码注释 | 有/无注释函数，输入输出和副作用有独立真值 | 简介有来源、限制与证据；不展开时可理解输入输出；不凭名字编造行为 |
| R3-07 | 审阅 read/search 依赖缓存 | 未完成 | 修改实际读取文件；在零结果搜索范围新增注册；不相关文件变化 | 相关审阅失效，不相关审阅复用；新增绑定不能因旧图无边而遗漏 |
| R3-08 | 锁事件、临界区与共享状态并行性 | 已完成（限定词法共同锁） | 两个持锁访问与一个无锁写入 fixture；查询 `locks` 与 HTML | 共同锁标 `serialized_by_common_lock/exact`；无共同锁标 `potential_race/may`；不声称证明运行时并发 |

本轮输出的精度边界须单列：支持哪些指针操作、哪些别名情况保守近似、哪些保持 opaque。不能承诺任意 C/C++ 程序都获得唯一精确目标。

## 7. 第 4 轮：Linux SDK 边界与对象生命周期

| ID | 本轮支持功能 | 是否完成 / 当前状态 | 验证方法（计划） | 预期结果 / 验收标准 |
|---|---|---|---|---|
| R4-01 | 公共 SDK 入口、ko 初始化/退出 | 未开始 | 导出接口、module_init/module_exit、封装宏样例 | 入口种类、注册证据、框架触发含义可见；不强行归并成唯一入口 |
| R4-02 | ioctl 用户态/内核态对应关系 | 未开始 | 用户 wrapper、设备节点、cmd 编号、file_operations 分发表 | 有设备与 cmd 证据才关联；相同 cmd 不同设备不误连；未知映射可审阅 |
| R4-03 | IRQ、workqueue、timer 等事件入口 | 未开始 | 注册/调度/取消的驱动样例，含多个 callback | 区分注册位置与执行触发；调度不冒充当场同步调用 |
| R4-04 | MMIO/寄存器读写边界与封装链 | 未开始 | readl/writel 及项目包装函数；地址/偏移/写值传递 | 显示寄存器访问操作和参数来源；无寄存器手册时不编造硬件位域含义 |
| R4-05 | 对象注册、使用、更新、注销生命周期 | 部分完成，仅原始引用/写入 | 静态对象、回调表、条件初始化、注销后可能使用 | 对象独立区域展示关联事件、所在函数、条件与顺序约束 |
| R4-06 | 规则版本和增量失效 | 未开始 | 更换某一框架规则、修改注册表、依赖仓变更 | 只重算使用该规则/证据的相关结果，保持其他事实与简介缓存 |

“什么时候”指源码位置、触发条件和可证明的先后关系；静态分析不输出虚构的运行时间戳。llama.cpp 不包含这些内核场景，必须增加可公开复现驱动样例，并在具备输入条件后对用户真实 SDK 验收。

## 8. 第 5 轮：维护、恢复、HTML 与完整原型验收

| ID | 本轮支持功能 | 是否完成 / 当前状态 | 验证方法（计划） | 预期结果 / 验收标准 |
|---|---|---|---|---|
| R5-01 | 文件→TU→摘要→规则→审阅→导出的失效传播 | 部分完成，仅 TU 等基础层 | 改实现/头/宏/工具/规则/依赖仓/搜索集合的更新矩阵 | 每类变更有依赖证据与重算解释；新增关系不漏；不无故全仓重解析 |
| R5-02 | 增量与干净全量的一致性 | 未开始系统验收 | 固定配置/工具/规则，比较 fresh 与 incremental；Agent 使用录制响应 | 规范化后的确定性事实及派生关系等价；不要求新生成自然语言逐字相同 |
| R5-03 | Checkpoint、中断、损坏与并发恢复 | 部分完成 | 在配置/解析/合并/审阅/发布阶段注入中断，破坏单个缓存对象，并发启动写者 | 已提交结果复用；损坏对象重算；半成品不发布；旧 current 在失败时仍有效 |
| R5-04 | HTML 黑盒/细节/分歧/参数流/对象区交互 | 部分完成，尚缺浏览器验收 | 浏览器自动化：搜索、展开、选择分歧、查看证据、追踪字段、循环图 | 展示与统一数据一致；离线不访问网络；详情按点击展开，不重复调用 Agent |
| R5-05 | 假设、来源、unknown、partial 和覆盖显示 | 部分完成 | 混合 exact/may/Agent 关系、失败 TU、预算截断、替代配置 | 不确定性可见可追溯；空数据不冒充已分析无结果 |
| R5-06 | Windows/Linux 发行包与运行时验证 | 部分完成 | 支持矩阵内干净主机、含空格路径、只读 Skill 目录/可写缓存、缺依赖 | 明确支持环境内可启动；权限限制或项目依赖缺失有精确诊断；不宣称任何环境通用 |
| R5-07 | 规模与性能基线 | 未开始系统验收 | 固定硬件/版本，分别测轻量接口、仓库分析、单 commit 更新和大 HTML | 报告 TU、节点、内存、耗时、缓存命中；依据实测制定预算，不预填无依据性能数 |
| R5-08 | 用户完整原型验收与正式架构确认 | 未开始，待前置交付 | 按原始需求清单演示真实 SDK 场景，记录用户反馈与决策 | 功能边界和交互获得确认；架构 ADR 明确后才进入第 6 轮 |

## 9. 第 6 轮：正式 VSCode 插件

本轮开始条件：第 5 轮功能验收通过，且用户已确认正式代码架构。Skill 原型代码不等于此门禁已满足。

| ID | 本轮支持功能 | 是否完成 / 当前状态 | 验证方法（计划） | 预期结果 / 验收标准 |
|---|---|---|---|---|
| R6-01 | 扩展骨架与 Python 服务协议 | 未开始 | VSCode 扩展宿主测试、协议版本兼容/不兼容对照 | 命令调用成功；协议错误明确提示；插件不自行解析源码 |
| R6-02 | Remote SSH / 远端 Linux 工作区 | 未开始 | Windows VSCode 连接 Linux 仓库，启动分析并查看结果 | 解析在代码所在主机执行；本地无需复制整仓与目标编译环境 |
| R6-03 | 仓库/接口入口、参数补齐与任务进度 | 未开始 | 创建任务、补参数、暂停/取消/恢复、服务重启 | 进度有真实阶段依据；已提交 checkpoint 保留；取消不会发布半成品 |
| R6-04 | Webview 图交互和源码跳转 | 未开始 | 与离线 HTML 对照，测试当前/旧 commit 证据位置 | 相同数据得到相同关系；版本不匹配不静默跳到错误行 |
| R6-05 | commit 更新、局部分析请求与导出 | 未开始 | 在扩展内改源码/切 commit/展开未分析函数 | 调用 Python 增量任务；未完成范围可见；导出绑定具体快照 |
| R6-06 | VSIX 打包、安装、升级与回归 | 未开始 | 支持平台的干净安装与版本升级，运行关键场景 | 可安装与启动；缓存/schema 升级有迁移或明确重算策略 |

本轮交付：源代码、VSIX、安装说明、远端运行说明、兼容矩阵与回归报告。是否发布扩展市场是另一个明确动作，不由开发完成自动触发。

## 10. 每轮交付与状态更新要求

每个功能 ID 在合入时附以下记录；未执行时填写“未执行”，不要用预期结果代替实测结果。

| 字段 | 必填内容 |
|---|---|
| 功能 ID / 本轮 | 如 R2-03 / 第 2 轮 |
| 支持范围 | 已覆盖的语法、配置、平台和数据规模；明确不支持范围 |
| 实现状态 | 未开始 / 部分完成 / 已实现待验证 / 已完成（限定范围） |
| 代码证据 | PR 链接、合入 commit、主要模块 |
| 输入环境 | 样例或仓库 commit、编译配置、依赖版本、工具/规则版本、平台 |
| 验证方法 | 可复制命令、测试函数、浏览器操作或故障注入步骤 |
| 预期结果 | 具体节点/关系/条件/状态、复用和重算集合或失败行为 |
| 实际结果 | 运行输出、断言、截图或 artifact 链接；含失败与限制 |
| 缓存与恢复 | 新产物依赖、失效规则、checkpoint 粒度及验证结果 |
| 用户验收 | 不需要 / 未开始 / 待验收 / 已确认，附确认依据 |

每轮至少提交：代码、测试/样例、实际验证报告、更新后的本矩阵和能力说明。改变中间文件或 HTML 数据契约时同步更新总体设计；主版本不兼容变更必须给出迁移或重算路径。

## 11. 下一步执行范围

下一轮先扩展 R3 的字段/别名和回调基础。锁分析已经交付“共同词法锁 + 静态/全局共享对象”的限定子集，下一步才扩展跨函数锁所有权、线程入口和条件变量；同时继续补齐第 2 轮未支持的表达式边界。R1、R2 的实际验证以 update/001、update/002 为准。保留现有 HTML 输出边界，不提前把正式 VSCode 扩展作为主开发对象。

本计划的初始表格用于需求对照；每轮实际交付、是否完成及验证记录以 update/ 对应文档为准。
